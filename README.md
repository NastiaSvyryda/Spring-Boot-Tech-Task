# SprinBoot test task

### Usage:

```
Docker Compose must be installed for starting postgresql
1. git clone https://github.com/NastiaSvyryda/Spring-Boot-Tech-Task.git
2. cd Spring-Boot-Tech-Task
3. start postgresql: docker-compose up -d
4. ./mvnw clean package spring-boot:run
```